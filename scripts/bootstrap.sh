#!/usr/bin/env bash
# Bootstrap PlanForge (install deps, build packages)

set -e
cd "$(dirname "$0")/.."

echo "Bootstrapping PlanForge..."
# pnpm install
# pnpm -r build
# pip install -e packages/cli-py
echo "Done."

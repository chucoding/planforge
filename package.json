{
  "name": "planforge",
  "version": "0.1.0",
  "private": true,
  "description": "Bring your own AI to Cursor",
  "scripts": {
    "bootstrap": "bash scripts/bootstrap.sh",
    "build": "pnpm -r build",
    "build:cli": "pnpm --filter planforge run build",
    "install:global": "pnpm run build:cli && npm install -g ./packages/cli-js",
    "planforge": "node packages/cli-js/dist/index.js"
  },
  "devDependencies": {}
}

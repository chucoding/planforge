"""Copy PlanForge templates (skills, rules, config) into project."""


def install_templates(target_dir: str) -> None:
    # TODO: copy from packages/core or templates/ into target_dir
    print("install templates to", target_dir)

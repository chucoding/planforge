"""planforge install - install Cursor slash commands and templates."""


def run_install(args: list[str]) -> None:
    print("planforge install")
    # TODO: copy templates to .cursor, register commands

/**
 * Copy PlanForge templates (skills, rules, config) into project
 */

export async function installTemplates(targetDir: string): Promise<void> {
  // TODO: copy from packages/core or templates/ into targetDir
  console.log("install templates to", targetDir);
}
